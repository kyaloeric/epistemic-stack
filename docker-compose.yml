services:
  epistemic-stack:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Optional: only needed if you run the live pipeline (python -m src.run) inside the container.
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
