# Sub-question: "How much of the zoonosis conclusion rests on the Huanan market cluster?"

> Fill `baseline.md` and `pipeline.md` with the raw outputs, then complete this delta.
> Run after the live pipeline produces the real COVID graph.

## What the baseline (deep research / Claude Code) produced
*(paste output; note tool + date)*

Expected character: a fluent narrative that *mentions* the market cluster as important
evidence, discusses zoonosis vs lab-leak, and gives a bottom line — but does not quantify
how load-bearing the single claim is, and does not link each sub-claim to a verbatim span.

## What the pipeline produced
*(export from cases/covid/out/cruxes.json + the graph)*

Expected character: the market-cluster claim surfaced as the #1 crux with an explicit
sensitivity score and the dependency path showing which conclusions rest on it; the
"remove it and it flips" relationship made explicit (C004); every claim auditable to a span.

## The delta (honest)
- **Pipeline advantage:** makes the load-bearing structure explicit and auditable; a reader
  can see *why* one claim dominates and check each attestation.
- **Baseline advantage:** faster, more readable as prose; better for a first orientation.
- **Verdict:** state plainly whether the pipeline is "meaningfully better" on THIS sub-question,
  and where it is not.
